{
  "name": "@test/app",
  "version": "1.0.0",
  "scripts": {
    "build": "echo Clean dist && echo Build client && echo Build server && echo Generate manifest && echo Optimize assets",
    "test": "echo Unit tests && echo Integration tests",
    "dev": "echo Running dev server",
    "preview": "echo Preview build",
    "deploy": "echo Validate && echo Upload && echo Verify"
  },
  "dependencies": {
    "@test/api": "workspace:*",
    "@test/pkg#special": "workspace:*",
    "@test/shared": "workspace:*",
    "@test/ui": "workspace:*"
  }
}
