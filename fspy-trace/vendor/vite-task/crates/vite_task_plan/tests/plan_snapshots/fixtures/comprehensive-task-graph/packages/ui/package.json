{
  "name": "@test/ui",
  "version": "1.0.0",
  "scripts": {
    "build": "echo Compile styles && echo Build components && echo Generate types",
    "test": "echo Testing UI",
    "lint": "echo Linting UI",
    "storybook": "echo Running storybook"
  },
  "dependencies": {
    "@test/shared": "workspace:*"
  }
}
